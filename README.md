# Portfolio
A responsive developer portfolio showcasing projects, certificates, skills, and achievements with animated UI.

every thing is good

## Run locally

This project loads navbar, footer, and page sections from separate HTML files.
Open it with VS Code Live Server or run:

```bash
python -m http.server 5500
```

Then visit `http://127.0.0.1:5500/`.
